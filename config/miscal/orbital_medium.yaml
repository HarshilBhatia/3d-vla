# Per-sample draw from the per-group-level table. A single-entry list still
# reads per_group_levels, which holds a different realization than levels.medium.
miscal_mode: group
miscal_group_level: [medium]
