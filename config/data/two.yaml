# Two tasks (bimanual_lift_tray + bimanual_push_box). Paths relative to project root.
# Use: python main.py data=two
train_data_dir: Peract2_zarr/bimanual_lift_tray,bimanual_push_box/train.zarr
eval_data_dir: Peract2_zarr/bimanual_lift_tray,bimanual_push_box/val.zarr
train_instructions: instructions/peract2/instructions.json
val_instructions: instructions/peract2/instructions.json
