# Single task (e.g. bimanual_lift_tray). Paths relative to project root.
# Use: python main.py data=single
train_data_dir: Peract2_zarr/bimanual_lift_tray/train.zarr
eval_data_dir: Peract2_zarr/bimanual_lift_tray/val.zarr
train_instructions: instructions/peract2/instructions_bimanual_lift_tray.json
val_instructions: instructions/peract2/instructions_bimanual_lift_tray.json
train_iters: 30000
