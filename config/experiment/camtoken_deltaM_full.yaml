# Experiment: delta_m_full — full D×D RoPE mixing matrix (more expressive than 6×6 delta_m).
# Run: python main.py experiment=camtoken_deltaM_full

view_align_mode: rope_full
layerwise_view_align: true
